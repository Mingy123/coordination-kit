# coordination-kit

Button+foot-switch → ESP32 → LSL pipeline. Monorepo.

- `poc/` — laptop-based proof of concept (pygame UI, FTDI button, keyboard foot switch)
- `firmware/` — ESP32 Rust firmware (C6 + S3)
- `bridge/` — serial-to-LSL outlet script
